{
  "name": "rain",
  "version": "1.0.0",
  "description": "Rain drop effect with WebGL",
  "main": "dist/index.js",
  "scripts": {
    "build": "node ./build.js",
    "build:watch": "node ./build.js --dev -w",
    "start": "npx esbuild --serve=0.0.0.0:8000 --servedir=./",
    "check": "npx tsc",
    "check:watch": "npx tsc -w"
  },
  "author": "SardineFish",
  "license": "MIT",
  "dependencies": {
    "zogra-renderer": "^1.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.8.50",
    "typescript": "^4.1.5"
  }
}
